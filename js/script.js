
// function
function check() {

    // getting data
    let name = document.getElementById("name");
    let email = document.getElementById("email");
    let msg = document.getElementById("textarea");

    // alert
    alert("nama : " + name.value + ", " + "email : " + email.value + ", " + "Message : " + msg.value);

}
